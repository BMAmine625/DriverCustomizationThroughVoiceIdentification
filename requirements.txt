# NOTE : torch et torchaudio ne sont PAS dans ce fichier volontairement.
# Ils doivent être installés séparément en version CPU pour éviter tout
# mélange CPU/GPU (voir setup.sh ou README.md).
#
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# Ensuite seulement, installer le reste avec ce fichier :
#   pip install -r requirements.txt

librosa
scikit-learn
numpy
soundfile
speechbrain
sounddevice